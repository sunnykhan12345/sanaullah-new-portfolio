# Assets Source Folder

This folder is reserved for source/editable assets. Public assets used by Next.js are located in `public/assets`.

- Icons are implemented as inline SVG components in `components/common/Icon.js`.
- Project preview SVG files are in `public/assets/projects`.
- Testimonial avatar SVG files are in `public/assets/testimonials`.
